# Environment Configuration for BrainBoard Backend

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Origins (comma-separated for multiple origins)
FRONTEND_URL=http://localhost:3000,http://localhost:5173

# Production Frontend URLs (update these when deploying)
# FRONTEND_URL=https://your-frontend-app.onrender.com,https://your-custom-domain.com
